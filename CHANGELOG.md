# Changelog

## 1.0.0

- Initial release of .NET Fast Debug.
- Adds a `.csproj` context menu command to build and start a `coreclr` debug session.
- Creates or updates the workspace build task used before launching the debugger.
